#include <stdio.h>
int main()
{
   // Array declaration & initialization
   int marks[] = {50, 60, 70, 80, 90, 100}, i;
   printf("\n Marks are :  ");
   // printing the elements
   for (i = 0; i <= 5; i++)
      printf("%d\t", marks[i]);
}